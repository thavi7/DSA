#include<iostream>
using namespace std;

int main() {
 /*   int arr[1000];
    int n,i,target,ans;
    cout << "enter the size" << endl;
    cin >> n;
    cout << "enter the element" << endl;
    for (int i = 0; i < n; i++)
    {
        cin >> arr[i];
    }
    cout << "enter the target" << endl;
    cin >> target;
    int st=0,end=n-1,num1=-1,num2=-1;
  while (st<=end)
  {
    if ((long long) arr[st]*arr[end]==target)
    {
        num1=arr[st] , num2=arr[end];
        break;
    }
    else if ((long long)arr[st]*arr[end]>target)
    {
       end--;
    }
    else{
        st++;
    } 
  } 
    cout<<num1<<" & "<<num2; */
    
    
    return 0;
}